[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { workspace = true, default-features = false, features = ["bevy_color", "bevy_state"] }
serde = { workspace = true }
bincode = { workspace = true }
glam = { workspace = true }

